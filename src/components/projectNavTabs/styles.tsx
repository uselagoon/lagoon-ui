import { LoadingSkeleton } from '@uselagoon/ui-library';
import styled from 'styled-components';

export const ProjectDeployTargetsSkeleton = styled(LoadingSkeleton)`
  margin-left: 8px;
  border-radius: 100px;
  margin-right: 8px;
`;
