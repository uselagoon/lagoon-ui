import styled from 'styled-components';

export const TasksPageWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 18px;

  .selector {
    margin-top: -2rem;
    margin-bottom: 1rem;
  }
`;
