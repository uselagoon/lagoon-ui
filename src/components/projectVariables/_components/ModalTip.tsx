import { Tip } from '@uselagoon/ui-library';

const ModalTip = () => {
  return (
    <Tip
      content={
        <>
          Create a unique name for your variable. Then choose the scope of the variables availability. For more
          information see our documentation
        </>
      }
    />
  );
};

export default ModalTip;
